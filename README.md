# Boost Beast CMake Project Template

### Install Ubuntu Requirements

```bash
sudo apt-get update && sudo apt install libssl-dev libcurl4-openssl-dev libboost-all-dev
```

### Install Windows Requirements

```bash
choco install openssl -y
```